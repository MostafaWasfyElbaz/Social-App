export enum Events {
  confirmEmail = "confirmEmail",
  resetPassword = "resetPassword",
}

export enum Gender {
    male = "male",
    female = "female",
}

export enum TokenType {
    access = "access",
    refresh = "refresh",
}